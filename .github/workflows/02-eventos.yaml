name: 02 - Events
on: 
    schedule:
        - cron: '6 * * * *'
    #push:
    #workflow_dispatch:

jobs:
    job: 
        runs-on : ubuntu-latest
        steps:
            - name: ShowTrigger
              run: |
                echo "Event name: ${{ github.event_name}}"

    

    